import React from 'react';
import styles from './HiddenCard.module.css'


const HiddenCard: React.FC = () => {
  return (
    <div className={styles.hiddenCard}>
      ?
    </div>
  );
};

export default HiddenCard
